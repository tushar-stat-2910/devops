name: learn_yml_file_writing

on:
  push:
    paths: 
      - "learn_yml.yml"
      
jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: step 1
        run: echo "hello"

      - name: step 2
        run : echo "Learning YAML"
        
  
